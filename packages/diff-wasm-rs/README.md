# diff-wasm-rs

Rust/WASM backend for Mohi diffing. This is the first cut: a minimal wasm-bindgen export that returns a replace op when HTML differs.

Build with wasm-pack or your preferred wasm toolchain.
